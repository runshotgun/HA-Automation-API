# This profile is currently unused because config.yaml sets apparmor: false.
profile ha_automation_api flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  network,
  capability,
  file,

  /config/** rwk,
  /tmp/** rw,
  /usr/src/** r,
  /usr/bin/node ix,
}
